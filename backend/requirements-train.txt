# ─────────────────────────────────────────────────────────────────────────────
# TRAINING / RESEARCH requirements.
#
# Install on a training machine (NOT on the 512MB Render serving instance):
#     pip install -r requirements.txt -r requirements-train.txt
#
# Everything here is used to TRAIN, tune, explain, or run the local Streamlit
# dashboard — none of it is imported when the API merely serves predictions.
# ─────────────────────────────────────────────────────────────────────────────

# ── Hyperparameter tuning ───────────────────────────────────────────────────────
optuna>=3.5.0

# ── Deep Learning (Temporal Fusion Transformer, FinBERT sentiment) ──────────────
# Install PyTorch with the right CUDA build per SETUP.md if you want GPU.
torch>=2.1.0
pytorch-forecasting>=1.0.0
pytorch-lightning>=2.1.0
transformers>=4.36.0

# ── Explainability (offline analysis; serving uses native TreeSHAP) ─────────────
shap>=0.44.0

# ── Local dashboard / plotting ──────────────────────────────────────────────────
streamlit>=1.31.0
plotly>=5.18.0

# ── Stats / research ────────────────────────────────────────────────────────────
statsmodels>=0.14.0
tqdm>=4.66.0

# ── Optional caching / extra news source (not required by the live API) ─────────
redis>=5.0.1
newsapi-python>=0.2.7
